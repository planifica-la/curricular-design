<%= form_with(model: fundamental_competence) do |form| %>
  <% if fundamental_competence.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(fundamental_competence.errors.count, "error") %> prohibited this fundamental_competence from being saved:</h2>

      <ul>
        <% fundamental_competence.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
