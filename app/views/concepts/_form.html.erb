<%= form_with(model: concept) do |form| %>
  <% if concept.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(concept.errors.count, "error") %> prohibited this concept from being saved:</h2>

      <ul>
        <% concept.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :concept_topic_id, style: "display: block" %>
    <%= form.text_field :concept_topic_id %>
  </div>

  <div>
    <%= form.label :concept, style: "display: block" %>
    <%= form.text_area :concept %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
